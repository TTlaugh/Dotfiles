#!/usr/bin/env bash
DIR="."
TOTAL=0
mapfile -t files < <(find "$DIR" -type f -name "$1")
for FILE in "${files[@]}"; do
     count=$(grep -c ^ < "$FILE")
     # echo "$FILE has $count lines"
     echo -e "$count\t$FILE"
     TOTAL=$(( TOTAL+count ))
done
echo "TOTAL LINES COUNTED: $TOTAL"
